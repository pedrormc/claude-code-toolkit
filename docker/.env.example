# ── Required ──
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Workspace ──
# Path to the project directory on the host that Claude can access
# This is the ONLY host directory mounted inside the container
WORKSPACE_PATH=./workspace

# ── Optional: n8n integration ──
# N8N_API_URL=https://your-n8n-instance.com
# N8N_API_KEY=your-n8n-api-key

# ── Optional: headless mode ──
# CLAUDE_PROMPT="Review the codebase for security issues"
# CLAUDE_ALLOWED_TOOLS=Read,Glob,Grep,Bash,Edit,Write
